<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.finale.dao.FollowDAO">


	<!-- 해당 유저를 팔로우하고 있는 유저들 리스트 찾기 -->
	<select id="selectFollowedByUser" resultType="User">
		SELECT b.*
		FROM
		follows a
		left join users b
		on a.following_id = b.user_id
		where
		a.followed_id = #{user_id};
	</select>

	<!-- 해당 유저가 팔로우하고 있는 유저들 리스트 찾기 -->
	<select id="selectFollowingByUser" resultType="User">
		SELECT b.*
		FROM
		follows a
		left join users b
		on a.followed_id = b.user_id
		where
		a.following_id = #{user_id};
	</select>

	<!-- 팔로우 -->
	<insert id="insertFollow" parameterType="follow">
		insert into follows(
		followed_id, following_id)
		values (#{followed_id},#{following_id});
	</insert>


	<!-- 팔로우 해제 -->
	<delete id="deleteFollow" parameterType="int">
		delete
		from follows
		where
		follow_id = #{follow_id}
	</delete>

</mapper>

